as_monomial:

controlla primo elemento:
	  -se e' asterisco, e' un prodotto di piu variabili
	  -se e' un numero e basta, (m numero 0 ())
	  -se e' diviso, deve essere fatto solo di numeri moltiplicati e divisi
	  -se e' una variabile, e' solo quella variabile - tipo x, y^2, u^3
	  -altrimenti errore
